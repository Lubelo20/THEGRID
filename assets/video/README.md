# Video assets

Drop the journey-explainer video here so the home-page player works automatically:

- `journey.mp4`  (H.264 MP4, 16:9, ≤10MB)
- `journey.webm` (VP9 WebM, same content)

Referenced by `index.html` → the `<video class="video-frame">` element under "One ecosystem, end to end."
Also update its `poster` to a real still frame. See item E1 in docs/ASSET-REQUIREMENTS.md.
